# 任務：補充單元測試至核心覆蓋率 ≥ 70%

## 現狀（基準 commit `c1d2016`）
- 7 suites / 60 tests，全過
- 缺口：host-agent（0%）、db（0%）、task-scheduler（0%）、handoff-service（P5 後新增）
- `import.meta.url` 在 ts-jest CJS 下不可用，須用 dynamic import workaround

## 新增測試計劃

### 1. `tests/unit/host-agent.test.ts` 🔴 必要
| 項目 | 測試數 | 重點 |
|------|--------|------|
| SSE 解析 | 5 | 正常 chunk、`[DONE]`、中途截斷、malformed JSON、空 data |
| tool call loop | 4 | 正常執行→回傳、多輪 tool call、MAX_TOOL_ROUNDS 觸頂、tool error |
| rateLimitedFetch | 4 | 正常回應、429 重試 ≤3次、超過後 throw、非 429 error |
| message history | 2 | 截斷超過 MAX_HISTORY、空歷史 |

Mock: `global.fetch`，tool executor stub

### 2. `tests/unit/db.test.ts` 🟡 重要
| 項目 | 測試數 | 重點 |
|------|--------|------|
| message CRUD | 4 | store / getById / getSince / getRecent |
| session 管理 | 3 | get / update / 不存在時建立 |
| migration | 2 | schema v1→v2、全新 DB |

使用 `:memory:` SQLite，零 side effect

### 3. `tests/unit/commands.test.ts` 🟡 重要
| 項目 | 測試數 | 重點 |
|------|--------|------|
| applySemeowShortcuts | 3 | 匹配/不匹配/非瑟喵助手 |
| handleHandoffButton | 2 | 正常/未註冊群組 |
| handleDraw | 3 | 正常/空 prompt/tool not found |

Mock: grammy Context、config、getToolHandler

### 4. `tests/unit/handoff-service.test.ts`（P5 完成後）
| 項目 | 測試數 | 重點 |
|------|--------|------|
| formatHandoffLines | 4 | 完整/無 commit/無變更檔/空 summary |

純函式，無 mock。

## 前置條件
- P4 完成（型別穩定後才寫 mock）
- P5 完成（handoff-service 才存在）

## 驗證
```bash
npx jest --runInBand --coverage
# 目標：host-agent 語句覆蓋率 ≥ 70%、db ≥ 80%
# 總測試數 ≥ 90
```
