# 任務：消除全部 `any` 型別逃逸

## 現狀（基準 commit `c1d2016`）
`grep -rn "as any\|: any" src/` 共 16 處。

## 逐一修復清單

### 1. `src/host-agent.ts`（5 處）
| 行 | 現狀 | 修法 |
|----|------|------|
| L230 | `tool_calls?: any[]` | 定義 `ToolCall` interface 在 `src/types.ts` |
| L281 | `catch (err: any)` | → `catch (err: unknown)` + `err instanceof Error ? err.message : String(err)` |
| L333 | `const messages: any[]` | → `OpenRouterMessage[]`（定義在 `src/types.ts`） |
| L693 | `[] as any[]` | → `HandoffEntry[]` |
| L795 | `[] as any[]` | → `HandoffEntry[]` |

### 2. `src/heartbeat.ts`（3 處）
| 行 | 現狀 | 修法 |
|----|------|------|
| L29 | `pm2Procs: any[]` | 定義 `PM2Process` interface |
| L82 | `let botProcs: any[]` | → `PM2Process[]` |
| L88 | `const procs: any[]` | → `PM2Process[]` |

### 3. `src/tools/web-search.ts`（2 處）
| 行 | 現狀 | 修法 |
|----|------|------|
| L75 | `(await res.json()) as any` | 定義 `BraveSearchResponse` interface |
| L139 | `(await res.json()) as any` | 定義 `DDGSearchResponse` interface |

### 4. `src/decision-agent.ts`（1 處）
| 行 | 現狀 | 修法 |
|----|------|------|
| L194 | `(await response.json()) as any` | 定義 `DecisionAPIResponse` interface |

### 5. `src/health-monitor.ts`（1 處）
| 行 | 現狀 | 修法 |
|----|------|------|
| L49 | `catch (err: any)` | → `catch (err: unknown)` |

### 6. `src/telegram/commands.ts`（1 處）
| 行 | 現狀 | 修法 |
|----|------|------|
| L111 | `(p: any) => p.name` | → `(p: PM2Process) => p.name` |

### 7. `src/telegram/callback-handler.ts`（1 處）
| 行 | 現狀 | 修法 |
|----|------|------|
| L58 | `(a: any)` | → `(a: HandoffAction)`，定義在 auto-commit.ts |

### 8. `src/auto-commit.ts`（1 處）
| 行 | 現狀 | 修法 |
|----|------|------|
| L5 | `summary: any` | → `summary: HandoffSummary` |

### 9. 舊 `src/telegram.ts`（1 處）
此檔案已是死檔（index.ts 引用 `./telegram/index.js`），直接**刪除**。

## 新增型別（集中在 `src/types.ts`）
```ts
export interface ToolCall {
  id: string;
  function: { name: string; arguments: string };
}

export interface OpenRouterMessage {
  role: 'system' | 'user' | 'assistant' | 'tool';
  content: string;
  tool_calls?: ToolCall[];
  tool_call_id?: string;
}

export interface PM2Process {
  name: string;
  pid: number;
  pm2_env: { status: string; pm_uptime: number; restart_time: number };
  monit: { memory: number; cpu: number };
}

export interface HandoffEntry {
  time: string;
  group: string;
  summary: HandoffSummary;
}
```

## 驗證
```bash
npx tsc --noEmit           # 0 errors
grep -rn "as any\|: any" src/ --include="*.ts" | grep -v node_modules | grep -v "\.d\.ts"  # 0 results
npx jest --runInBand       # 60/60 pass
```
