# NanoClaw - Roo Code 行為規範

## 核心原則

1. **提供完整且正確的 XML 工具格式**
   - 所有 Tool Call 必須使用標準 XML 標籤格式
   - 嚴禁在 Tool Call 標籤前後添加額外解釋文字
   - 回應時直接輸出工具調用，不要預先說明「我將調用 XXX」

2. **穩定性規則**
   - 嚴禁在 Tool Call 標籤前後添加額外解釋文字
   - 保持回應簡潔，只輸出必要內容
   - 避免多餘的格式裝飾

## 工具調用格式

正確：
```
<tool_code>
<tool name="read">
<param name="filePath">/path/to/file.ts</param>
</tool>
</tool_code>
```

錯誤（嚴禁）：
```
我將讀取這個檔案...
<tool_code>...
```

或
```
<tool_code>...
</tool_code>
讓我解釋一下這個調用...
```

## 執行原則

- 直接執行任務，不做預先聲明
- 工具失敗時簡要說明問題
- 完成後給出結果摘要
